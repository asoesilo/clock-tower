<tr class="<%= 'info' if entry[:holiday] %>">
  <td style="width:200px">
    <%= project.try(:name) || "[Unknown Project]" %>
  </td>
  <td>
    <%= task.try(:name) || "[Unknown Task]" %>
    <% if entry[:holiday] && entry[:date] %>
      - 
      <% holiday = Holidays.on(entry[:date], :ca_bc).first %>
      <%= holiday[:name] %> (<%=  holiday[:date] %>)
    <% end %>
  </td>
  <td style="width:60px">
    <%= entry[:hours] %>
  </td>
  <td style="width:90px">
    <%= number_to_currency rate %>/hr
  </td>
  <td style="width:90px">
    <strong><%= number_to_currency cost %></strong>
  </td>
</tr>