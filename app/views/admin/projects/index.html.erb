<div class="container">
  <div class="row">
    <div class="col-md-2 vcenter">
      <h1>Projects</h1>
    </div>
    <div class="vcenter">
      <%= link_to "Create new project", new_admin_project_path %>
    </div>
  </div>

  <%= render 'layouts/show_alert' %>

  <%= render 'layouts/show_notice' %>

  <%= render 'admin/projects/project_error' %>

  

  <% if @projects && @projects.any? %>
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Created at</th>
        </tr>
      </thead>
      <tbody>
        <%= render @projects %>
      </tbody>
    </table>
  <% else %>
    <div>No projects</div>
  <% end %>
</div>